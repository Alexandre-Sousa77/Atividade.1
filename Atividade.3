Algoritmo: Problema 01

programa {
  funcao inicio() {
    //Declaação de variáveis 
    real nota1,nota2,nota3,nota4,media
    
    //Entrada de dados
    escreva ("imforme a primeira nota:")
    leia(nota1)
    eecreva("infotme a segunda nota:")
    leia(nota2)
    escreva("informe a terceira nota:")
    leia(nota3)
    escreva("informe a quarta nota:")
    leia(nota4)

    //Processamento 
    media = (nota1+nota2+nota3+nota4)/4

    //Saida de dados 
    escreva("a media é:",media)
  }
}

    
Algoritmo: Problema 02

programa {
  funcao inicio() {
    //Declaração de variaveis 
    real valor_do_mes1,valor_do mes2,valor_do_mes3,media 

    //Entrada de dados
    escreva("informe o valor do primeiro mês")
    leia(valor1)
    escreva("informe o valor do segundo mês")
    leia(valor2)
    escreva("informe o valor do terceiro mês")
    leia(valor3)

    //Processamento 
    media = (valor1+valor2+valor3)/3
     
     //Saida de dados
     escreva("a media é:",media)
  }
}

Algoritimo:Problema 03

programa {
  funcao inicio() {

   //DeclaraÃ§Ã£o de variaveis 
   real altura,peso_ideal

   //DeclaraÃ§Ã£o de constante
   const real k=72.7
   const inteiro b= 58

   //Entrada de dados
   escreva("informe a altura do homem:")
   leia(altura) 


   //ProÃ§essamento
   peso_ideal=(k * altura)-b

   //Saida de dados
   escreva("o peso ideal de e:", peso_ideal)
  }
}

Algoritimo:Problema 04

programa {
  funcao inicio() {
  
    // Declaração de variaveis
    real valor_x,valor_y,troca

    // Entrada de dados
    escreva("informe o valor de x:")
    leia(valor_x)

    escreva("informe o valor de y:")
    leia(valor_y)

    // Proçessamento
    troca= valor_x
    valor_x= valor_y
    valor_y= troca

    // Saida de dados
    escreva("o valor de x e:",valor_y)
    escreva(" o valor de y e:",troca)
  }
}


Algoritimo:Problema 05

programa {
  funcao inicio() {
    
    // DeclaraÃ§Ã£o de variaveis
    real raio,diametro,comprimento,aria

    // Declaração  de costante 
    const real Pi= 3.14

    // Entrada de dados
    escreva("informe o valor do raio:")
    leia(raio)

    // Proçessameto
    diametro= 2*raio
    comprimento= Pi*raio
    aria= Pi *(raio*raio)

    // Saida de dados
    escreva("valor do diametro e:", diametro)
    escreva(" valor do comprimento e:", comprimento)
    escreva(" valor do raio e:", raio)
  }
}

Algoritimo:Problema 06

programa {
  funcao inicio() {
    // DecaraÃ§Ã£o de variaveis 
    real temperatura_celsius, temperatura_fahrenheit

    // Entrada de dados
    escreva("informe o valor em celsius: ")
    leia(temperatura_celsius)

    // ProÃ§essameto
    temperatura_fahrenheit= temperatura_celsius *(9.0/5.2) + 32.0

    // Saida de dados
    escreva("o valor da tempertura em graus celsius e:",temperatura_celsius )
    
  }
}
